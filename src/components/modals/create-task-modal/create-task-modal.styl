.modal-overlay
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.5);
    display: flex;
    justify-content: center;
    align-items: center;
    .modal-content
        background #FFF
        width 40%
        height 70%
        max-width 660px
        max-height 488px
        padding 2%
        border-radius 8px
        display flex
